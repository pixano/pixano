# =====================================
# Copyright: CEA-LIST/DIASI/SIALV/LVA
# Author : pixano@cea.fr
# License: CECILL-C
# =====================================

name: Test and Publish Docker Image

on:
  push:
    branches:
      - main

env:
  TEST_TAG: pixano/pixano:test
  LATEST_TAG: pixano/pixano:latest

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push test
        uses: docker/build-push-action@v6
        with:
          load: true
          tags: ${{ env.TEST_TAG }}

      - name: Test
        run: |
          docker run -p 8000:8000 --name pixano -d -v ./tests/assets/library:/app/library ${{ env.TEST_TAG }}
          sleep 5
          EXPECTED_RESPONSE='[{"id":"dataset_image_bboxes_keypoint","name":"dataset_image_bboxes_keypoint","description":"Description dataset_image_bboxes_keypoint.","size":"Unknown","preview":"","num_items":5},{"id":"dataset_multi_view_tracking_and_image","name":"dataset_multi_view_tracking_and_image","description":"Description dataset_multi_view_tracking_and_image.","size":"Unknown","preview":"","num_items":5}]'
          RESPONSE=$(curl http://localhost:8000/datasets/info)
          if [ "$RESPONSE" == "$EXPECTED_RESPONSE" ]; then
              echo "Test Passed: Service returned expected response."
              echo "Expected: $EXPECTED_RESPONSE"
              echo "Actual: $RESPONSE"
              docker kill pixano
              docker rm pixano
              exit 0
          else
              echo "Test Failed: Service response did not match expected response."
              echo "Expected: $EXPECTED_RESPONSE"
              echo "Actual: $RESPONSE"
              docker kill pixano
              docker rm pixano
              exit 1
          fi

      - name: Build and push latest
        uses: docker/build-push-action@v6
        with:
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ env.LATEST_TAG }}
